# 统一换行符，避免在 Windows 上用 autocrlf 提交后把 CI 的 shell 脚本搞坏。
# CI 跑在 Linux 上，.sh 必须是 LF，否则 bash 会因为 \r 报错。
* text=auto eol=lf
*.sh text eol=lf
*.bash text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.java text eol=lf
*.gradle text eol=lf
*.properties text eol=lf
*.md text eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制：不要做任何换行符转换
*.jar binary
*.png binary
*.jpg binary
*.zip binary
*.gz binary
